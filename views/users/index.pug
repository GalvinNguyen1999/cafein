h1 List Users

a(href="/users/create") Create New 
form(action="/users/search", method="GET") 
  input(type="text", name="q", value=valueInput)
  button Search
ul 
  each user in users
    li #{user.name} and #{user.age}